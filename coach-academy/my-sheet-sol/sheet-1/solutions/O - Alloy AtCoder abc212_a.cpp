#include <bits/stdc++.h>
using namespace std;

int main()
{
  int g, s;
  cin >> g >> s;
  if (g == 0 || s == 0)
  {
    cout << (g == 0 ? "Silver" : "Gold");
    return 0;
  }

  cout << "Alloy";

  return 0;
}